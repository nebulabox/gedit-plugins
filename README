General Information
===================

This is version 3.15.1 of gedit-plugins. gedit-plugins are a set of plugins
for gedit.

Installation
============

For the moment, you have to ./configure with --prefix=same prefix as you
configured gedit with.

